- delete the .git from the repo : Show the hiddens files ( macOS: Command+shift+'.')
- cd <project-name>/packages and navigate into 
1. backend: `yarn install` and `yarn deploy`
1. frontend: `yarn install` and `yarn dev`